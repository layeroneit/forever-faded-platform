# Forever Faded API — dev environment
# Copy to .env and adjust. All fields support easy customization.

# Database (SQLite for dev; no install required)
DATABASE_URL="file:./dev.sqlite"

# JWT (dev secret is fine for local testing)
JWT_SECRET=dev-secret-change-me
JWT_EXPIRES_IN=7d

# Stripe (optional for dev — leave empty to skip online payments)
# Get keys at https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=

# Server
PORT=3001
NODE_ENV=development
